# 通用配置
# 注意：不仅 Nest.js 会读取此配置，项目中依赖的其他工具也可能读取，例如 simple-git 

# 存储 Git 仓库的目录
# 注意：Docker 中运行时，~ 的目录为 /root
GIT_REPO_STORAGE_PATH = ~/.paperplane-api/git-repos

# Git 使用的全局命令（simple-git 会读取此配置）
# GIT_SSH_COMMAND = 'ssh -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no'
